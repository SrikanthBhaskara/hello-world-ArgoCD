mvn clean install -DskipTests=ture --> skipping testcases

mvn clean install -DskipTests --> skipping testcases

mvn install -Dmaven.test.skip=true --> skipping test classes compilation

mvn test-compile --> compile testcases

mvn test -Dtest=Test1#methodname  --> Run a single test method from a test class.

mvn test // Run all the unit test classes

mvn test -Dtest=Test1 // Run a single test class

mvn test -Dtest=Test1,Test2 // Run multiple test classes

mvn test -Dtest=Test1#testFoo* // Run all test methods that match pattern 'testFoo*' from a test class.

mvn test -Dtest=Test1#testFoo*+testBar* // Run all test methods match pattern 'testFoo*' and 'testBar*' from a test class.

mvn -Dtest=DLPScannerEngineControllerTestIT test -DskipTests --> compile particular test class(DLPScannerEngineControllerTestIT) // -DskipTests  skipping test execution

========================

# Go to project directory
cd RegexTester

# Build the project
mvn clean package

# Run the program
mvn exec:java -Dexec.mainClass="com.example.RegexTester"

============

Linux:

🔥 CLI Args
Process only one policy:

bash
Copy
Edit
mvn exec:java \
  -Dexec.mainClass="com.example.RegexTesterMultiRule" \
  -Dexec.args="/path/to/policy.json ITpolicy"
📦 No Args
Process all policies:

bash
Copy
Edit
mvn exec:java \
  -Dexec.mainClass="com.example.RegexTesterMultiRule"


compile single class file mvn:::  mvn compile -Dchanged.classes= com/example/MyClass.java
=========================


windows:


mvn exec:java -Dexec.mainClass="RegexTester.RegexTesterHybrid" -Dexec.args="C:\Users\srikbhas\Downloads\2521346.json ITpolicySrikanth"

=================

Environment	Command
Windows CMD	mvn exec:java -Dexec.mainClass="RegexTester.RegexTesterMultiRule" -Dexec.args="C:\\path\\test.json ITpolicy"
Windows PowerShell	mvn exec:java -Dexec.mainClass="RegexTester.RegexTesterMultiRule" -Dexec.args='C:\path\test.json ITpolicy'
Linux Bash	mvn exec:java -Dexec.mainClass="RegexTester.RegexTesterMultiRule" -Dexec.args="/path/to/test.json ITpolicy"

💻 Windows CMD
cmd
Copy
Edit
java -jar RegexTester-0.0.1-SNAPSHOT.jar "C:\Users\test\Downloads\test.json" ITpolicy
💻 Windows PowerShell
powershell
Copy
Edit
java -jar RegexTester-0.0.1-SNAPSHOT.jar 'C:\Users\test\Downloads\test.json' ITpolicy
📌 Single quotes recommended if path contains spaces.

🐧 Linux / MacOS
bash
Copy
Edit
java -jar RegexTester-0.0.1-SNAPSHOT.jar "/home/srikanth/test.json" ITpolicy
📦 If No Arguments Provided
Your code will fallback to:

Load policy.json from resources

Process all rules

So you can also run without parameters:

bash

java -jar RegexTester-0.0.1-SNAPSHOT.jar
⚡ Pro Tip: Paths with Spaces
If JSON file path has spaces like:
C:\Users\test\My Documents\test.json

Windows
cmd
Copy
Edit
java -jar RegexTester-0.0.1-SNAPSHOT.jar "C:\Users\test\My Documents\test.json" ITpolicy
Linux
bash
Copy
Edit
java -jar RegexTester-0.0.1-SNAPSHOT.jar "/home/srikanth/My Documents/test.json" ITpolicy
🎯 Summary Table
Platform	Example Command
Windows CMD	java -jar RegexTester-0.0.1-SNAPSHOT.jar "C:\path\to\test.json" ITpolicy
Windows PowerShell	java -jar RegexTester-0.0.1-SNAPSHOT.jar 'C:\path\to\test.json' ITpolicy
Linux / MacOS	java -jar RegexTester-0.0.1-SNAPSHOT.jar "/path/to/test.json" ITpolicy
Both	(Fallback mode) java -jar RegexTester-0.0.1-SNAPSHOT.jar (loads policy.json from resources)
---
🚀 Solution: Tell Java which main class
You can do it in two ways:

✅ 1. Pass the Main Class on the Command Line
Instead of -jar, use -cp (class path) and specify the main class fully qualified:

💻 Windows
cmd
Copy
Edit
java -cp RegexTester-0.0.1-SNAPSHOT.jar RegexTester.RegexTesterMultiRule "C:\Users\test\Downloads\test.json" ITpolicy
🐧 Linux
bash
Copy
Edit
java -cp RegexTester-0.0.1-SNAPSHOT.jar RegexTester.RegexTesterMultiRule "/home/srikanth/test.json" ITpolicy
Here:

RegexTester.RegexTesterMultiRule is your package name + class name.

✅ 2. Set the Main Class in the JAR Manifest (Preferred)
If you want to keep using java -jar ..., then you need to make the JAR’s META-INF/MANIFEST.MF specify which class is the entry point.

📦 Edit pom.xml
Add this plugin:

xml
Copy
Edit
<build>
    <plugins>
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-jar-plugin</artifactId>
            <version>3.3.0</version>
            <configuration>
                <archive>
                    <manifest>
                        <mainClass>RegexTester.RegexTesterMultiRule</mainClass>
                    </manifest>
                </archive>
            </configuration>
        </plugin>
    </plugins>
</build>
Replace RegexTester.RegexTesterMultiRule with your desired default main class.

🏃‍♂️ Then Rebuild
bash
Copy
Edit
mvn clean package
This creates a new RegexTester-0.0.1-SNAPSHOT.jar where MultiRule is set as the default main class.

✅ Now Run
bash
Copy
Edit
java -jar RegexTester-0.0.1-SNAPSHOT.jar "C:\path\to\test.json" ITpolicy
or

bash
Copy
Edit
java -jar RegexTester-0.0.1-SNAPSHOT.jar "/path/to/test.json" ITpolicy
No need to specify main class anymore. 🎯

🏆 Best Practice
Scenario	Recommendation
One class is “primary”	Set it in the manifest
You switch main classes often	Use -cp and specify
Want all dependencies inside	Use fat JAR (see below)



=========================
Skipping Tests
To skip running the tests for a particular project, set the skipTests property to true.

<project>
  [...]
  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>2.12.4</version>
        <configuration>
          <skipTests>true</skipTests>
        </configuration>
      </plugin>
    </plugins>
  </build>
  [...]
</project>
You can also skip the tests via command line by executing the following command:

mvn install -DskipTests
If you absolutely must, you can also use the maven.test.skip property to skip compiling the tests. maven.test.skip is honored by Surefire, Failsafe and the Compiler Plugin.

mvn install -Dmaven.test.skip=true
Skipping by default
If you want to skip tests by default but want the ability to re-enable tests from the command line, you need to go via a properties section in the pom:

<project>
  [...]
  <properties>
    <skipTests>true</skipTests>
  </properties>
  [...]
  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>2.12.4</version>
        <configuration>
          <skipTests>${skipTests}</skipTests>
        </configuration>
      </plugin>
    </plugins>
  </build>
  [...]
</project>
This will allow you to run with tests disabled by default and to run them with this command:

mvn install -DskipTests=false
The same can be done with the "skip" parameter and other booleans on the plugin.
==========================================================================================
https://maven.apache.org/plugins-archives/maven-surefire-plugin-2.12.4/examples/single-test.html
Running a Single Test
During development, you may run a single test class repeatedly. To run this through Maven, set the test property to a specific test case.

mvn -Dtest=TestCircle test
The value for the test parameter is the name of the test class (without the extension; we'll strip off the extension if you accidentally provide one).

You may also use patterns to run a number of tests:

mvn -Dtest=TestCi*le test
And you may use multiple names/patterns, separated by commas:

mvn -Dtest=TestSquare,TestCi*le test
Running a set of methods in a Single Test Class
With version 2.7.3, you can run only n tests in a single Test Class.

NOTE : it's supported for junit 4.x and TestNG.

You must use the following syntax

mvn -Dtest=TestCircle#mytest test
You can use patterns too

mvn -Dtest=TestCircle#test* test
As of surefire 2.12.1, you can select multiple methods (JUnit4X only at this time, patches welcome)

mvn -Dtest=TestCircle#testOne+testTwo test
====================
mvn dependency:tree dependency tree
mvn dependency:tree -Dverbose
mvn dependency:tree -Dexcludes=org.slf4j
mvn dependency:tree -Dincludes=org.slf4j
mvn dependency:tree -DoutputType=graphml -DoutputFile=dependency.graphml